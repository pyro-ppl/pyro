Parameters
==========

Parameters are variables that are tagged as parameters and stored in the param store in memory.
Parameters can also be saved and loaded from disk.

.. include:: pyro.params.rst 
